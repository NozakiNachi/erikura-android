<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="jp.co.recruit.erikura.presenters.activities.RegisterNameViewModel" />
        <variable
            name="handlers"
            type="jp.co.recruit.erikura.presenters.activities.RegisterNameEventHandlers" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ScrollView
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:background="@color/white"
            android:fillViewport="true">
            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" >

                <View
                    android:layout_width="match_parent"
                    android:layout_height="190dp"
                    android:background="@color/paleGrey">

                </View>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <Button
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="60dp"
                        android:layout_marginRight="20dp"
                        android:layout_gravity="right"
                        style="@style/nextButton"
                        android:enabled="@{viewModel.isNextButtonEnabled()}"
                        android:onClick="@{handlers::onClickNext}"
                        />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="24dp"
                        style="@style/titleLabel"
                        android:text="@string/registerName_caption" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        style="@style/fieldLabel"
                        android:layout_marginTop="40dp"
                        android:text="@string/lastName" />

                    <EditText
                        android:layout_width="match_parent"
                        android:layout_height="62dp"
                        style="@style/textField"
                        android:layout_marginTop="10dp"
                        android:text="@={viewModel.lastName}"
                        android:hint="@string/register_last_name_hint" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:visibility="@{viewModel.lastNameErrorVisibility}"
                        style="@style/errorField"
                        android:text="@={viewModel.lastNameErrorMsg}"
                        />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        style="@style/fieldLabel"
                        android:layout_marginTop="40dp"
                        android:text="@string/firstName" />

                    <EditText
                        android:layout_width="match_parent"
                        android:layout_height="62dp"
                        style="@style/textField"
                        android:layout_marginTop="10dp"
                        android:text="@={viewModel.firstName}"
                        android:hint="@string/register_first_name_hint" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:visibility="@{viewModel.firstNameErrorVisibility}"
                        style="@style/errorField"
                        android:text="@={viewModel.firstNameErrorMsg}"
                        />

                </LinearLayout>

            </FrameLayout>
        </ScrollView>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>